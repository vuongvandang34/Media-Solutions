import RegisterForm from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Register Business | Business Feedback CRM',
  description: 'Create a new business tenant and start managing customer surveys.',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
